name: "Waveshare 7.5\" V2"
width: 800
height: 480
color_mode: "1"   # 1-bit black/white (Pillow mode "1")
bg_color: 0
fg_color: 255
# Wiring metadata — used only for ESPHome firmware generation (Phase 6), ignored by the renderer.
esphome:
  model: "7.50in-bV2"
  rotation: "180°"
  board: esp32dev
  spi: { clk_pin: GPIO13, mosi_pin: GPIO14 }
  pins: { cs: GPIO15, dc: GPIO27, busy: GPIO25, reset: GPIO26 }
