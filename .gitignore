# Runtime artefacts — rendered PNGs are not source
eink-renderer/output/

# Fonts are downloaded at build time (see Dockerfile)
eink-renderer/fonts/

# Local notes — not part of the committed project
.notes/

# ESPHome secrets and build artifacts
esphome/secrets.yaml
esphome/.esphome/

# Vendored assets — fetched at Docker build time, not committed
inkwell/frontend/static/*.min.js

# Local-only dev helper (machine-specific HA host)
scripts/sync-to-ha.sh

# MCP server config — may contain connection secrets
.mcp.json

CLAUDE.md
project-context.md
